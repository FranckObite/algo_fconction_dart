String reverseUsingSplitAndJoin(String text) {
  final chars = text.split('');
  chars.le

  return chars.reversed.join(''); 
}
in.parse;
void main() {
  int z = 5;
  int resultat = fact(z);
  print("le factoriel de $z est $resultat");
  reverseUsingSplitAndJoin(String text) {
    final chars = text.split('');
    final bb = chars.reversed.toList();
    String c = " ";
    for (int i = 0; i <= bb.length - 1; i++) {
      if (bb[i] == " ") {

      }
    }
    return bb.toList();
  }

  print(reverseUsingSplitAndJoin("hello word nan"));
}
/* void main() {
  String ss = "hello word";
  var g = ss.reversed.join();

  print();
  print(ss.split(" ").join());
}
 */
